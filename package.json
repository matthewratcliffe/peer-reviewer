{
  "name": "peer-reviewer",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev:service": "npm run dev --workspace packages/service"
  }
}
